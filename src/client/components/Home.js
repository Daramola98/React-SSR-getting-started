/* eslint-disable react/jsx-filename-extension */
import React from 'react';

const Home = () => (
  <div>
    <div> I am the Home Component </div>
    <button type="button" onClick={() => console.log('I was pressed')}> I can be pressed </button>
  </div>
);

export default Home;
